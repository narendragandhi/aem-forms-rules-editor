/**
 * AEM Forms Rules Editor - Comprehensive Custom Functions Library
 *
 * This is the entry point that initializes the shared namespace.
 * All functions are organized in separate category files:
 *
 *   validation.js     - US & international identity, contact, network, format validators
 *   formatting.js     - Phone, SSN, credit card, currency, date, name formatting
 *   financial.js      - Loan, interest, tax, discount, payment calculations
 *   date-utils.js     - Date validation, manipulation, and calculation
 *   string-utils.js   - Text processing, case conversion, string utilities
 *   data-utils.js     - Base64, UUID, object flattening, query string utilities
 *   geolocation.js    - Location lookup, distance calculation, state utilities
 *   file-utils.js     - File type validation, extension, size formatting
 */

(function (window) {
    "use strict";

    // Initialize the global namespace for all custom form rules
    window.CustomFormRules = window.CustomFormRules || {};

})(window);
